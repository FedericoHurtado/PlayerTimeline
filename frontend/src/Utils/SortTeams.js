/**
 * Helper function to sort a list of teams according to their standings.
 */
function sortTeams(teams, ascending = true) {
  // step 1: call API getTeamsFromLeague
  // step 2: ensure api resp code = 200
  // step 3: sort teams by winning % (sortTeamsFunction)
  // step 4: return sorted teams in a list
}

export default sortTeams;
